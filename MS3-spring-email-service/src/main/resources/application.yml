spring:
  application:
    name: MS3-spring-mail-appender

  mail:
    host: smtp.gmail.com
    port: 587
    username: pattanaikdebasnana@gmail.com
    password: aredecmnjmycwymg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 9090

management:
  endpoints:
    web:
      exposure:
        include: "*"

  endpoint:
    health:
      show-details: always